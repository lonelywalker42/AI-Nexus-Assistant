# 测试清单：v3.5.0 + v3.6.0 新特性

> 本文档列出 v3.5.0 和 v3.6.0 版本新增的所有待测试特性/功能及其入口。
> 测试完成后在每项后标记 ✅ 或 ❌，并记录问题。

---

## 一、v3.6.0 新特性（ScholarAIO 特性移植）

### 1.1 出版社 PDF 拉取

| # | 测试项 | 入口 | 预期结果 |
|---|--------|------|---------|
| 1 | DOI 拉取 PDF | 文献库页 → 工具栏 "📥 拉取 PDF" → 输入 DOI → 点击"开始拉取" | PDF 下载成功，自动提取元数据，论文入库 |
| 2 | 标题拉取 PDF | 同上弹窗 → 输入论文标题 | 通过 Crossref 解析 DOI 后拉取 |
| 3 | 批量拉取 | API: `POST /api/papers/batch-fetch-pdf` | 多个 DOI 逐个拉取，单篇失败不中断 |
| 4 | 重新拉取 | API: `POST /api/papers/{id}/refetch-pdf` | 对已有论文补充获取 PDF |
| 5 | 代理绕过 | 在开启 Clash 等代理环境下拉取 | 正常工作（proxy=None） |
| 6 | 非校园网提示 | 在非校园网环境拉取付费论文 | 返回明确错误信息 |

### 1.2 MinerU PDF→Markdown 转换

| # | 测试项 | 入口 | 预期结果 |
|---|--------|------|---------|
| 7 | MinerU 状态检测 | 设置页 → "PDF 转换引擎 (MinerU)" 区域 | 正确显示 ✅ 已安装 / ❌ 未安装 |
| 8 | MinerU 安装 | 同上 → 点击"安装 MinerU" | pip install 执行完成，状态刷新为已安装 |
| 9 | MinerU 转换 | API: `POST /api/papers/{id}/convert-markdown` | paper.md 生成，保留公式/图片/表格 |
| 10 | PyMuPDF 降级 | 未安装 MinerU 时触发转换 | 自动降级到 PyMuPDF 纯文本提取 |
| 11 | 批量转换 | API: `POST /api/system/batch-convert`（如已实现） | 所有 PDF 逐个转换 |

### 1.3 arXiv 集成

| # | 测试项 | 入口 | 预期结果 |
|---|--------|------|---------|
| 12 | arXiv 搜索 | 文献检索页 → 选择 arXiv 源 → 输入关键词搜索 | 返回 arXiv 论文列表，显示 arxiv_id 和分类 |
| 13 | arXiv PDF 导入 | 搜索结果 → arXiv 论文卡片 → 红色上传按钮 | PDF 下载 + 元数据提取 + 入库成功 |
| 14 | arXiv API 限速 | 连续导入多篇 arXiv 论文 | 3 秒礼貌间隔，无 429 错误 |
| 15 | 已入库标记 | 导入后再搜索同一篇 | 应有去重机制（DOI 或 arxiv_id） |

### 1.4 多源导入（BibTeX/RIS）

| # | 测试项 | 入口 | 预期结果 |
|---|--------|------|---------|
| 16 | BibTeX 导入 | 文献库页 → 工具栏 "导入" ▾ → "📚 导入 BibTeX" → 选择 .bib 文件 | 解析成功，批量入库，显示导入数量 |
| 17 | RIS 导入 | 同上 → "📋 导入 RIS" → 选择 .ris 文件 | 解析成功，批量入库 |
| 18 | DOI 去重 | 导入包含已有 DOI 的 BibTeX 文件 | 跳过已存在论文，不重复入库 |
| 19 | 格式自动检测 | 导入无扩展名文件 | 根据内容自动判断 BibTeX/RIS |
| 20 | 边界情况 | 导入空文件/格式错误文件 | 返回明确错误提示 |

### 1.5 论文笔记系统

| # | 测试项 | 入口 | 预期结果 |
|---|--------|------|---------|
| 21 | 查看笔记 | 文献库页 → 点击论文 → 详情面板 "📝 笔记" 区域 | 显示已有笔记列表 |
| 22 | 添加笔记 | 同上 → 输入框输入内容 → 点击"添加"或回车 | 笔记添加成功，列表更新 |
| 23 | 删除笔记 | 笔记项 → 点击 ✕ 按钮 | 笔记删除成功 |
| 24 | 编辑旧笔记 | 点击"编辑" → 修改内容 → 点击"保存" | user_notes 字段更新 |
| 25 | 笔记持久化 | 关闭应用重启 | 笔记数据保留 |

### 1.6 元数据质量审计

| # | 测试项 | 入口 | 预期结果 |
|---|--------|------|---------|
| 26 | 执行审计 | 文献库页 → 工具栏 "🔍 审计" | 弹窗显示审计结果 |
| 27 | 统计卡片 | 审计弹窗顶部 | 正确显示 总论文/有问题/严重/中等 数量 |
| 28 | 问题列表 | 审计弹窗列表 | 按严重程度排序，显示问题类型 |
| 29 | 点击跳转 | 点击审计结果中的论文 | 关闭弹窗，打开对应论文详情 |
| 30 | 无问题提示 | 所有论文元数据完整时 | 显示"✅ 所有论文元数据质量良好" |

### 1.7 语义近邻推荐

| # | 测试项 | 入口 | 预期结果 |
|---|--------|------|---------|
| 31 | 相关论文显示 | 文献库页 → 点击论文 → 详情面板底部 "🔗 相关论文" | 显示相似论文列表（需先构建向量索引） |
| 32 | 相似度分数 | 相关论文列表 | 每项显示百分比相似度 |
| 33 | 点击跳转 | 点击相关论文 | 切换到该论文详情 |
| 34 | 未构建提示 | 未构建向量索引时 | 显示"向量索引未构建"提示 |
| 35 | 构建向量索引 | 文献库页 → 构建向量（或 API: `POST /api/papers/build-vectors`） | 索引构建成功 |

### 1.8 工作区限定搜索

| # | 测试项 | 入口 | 预期结果 |
|---|--------|------|---------|
| 36 | 工作区内搜索 | API: `GET /api/workspaces/{id}/search?q=xxx` | 仅返回工作区内匹配论文 |

---

## 二、v3.5.0 新特性（科研 Agent + 基础增强）

### 2.1 科研 Agent 工作流

| # | 测试项 | 入口 | 预期结果 |
|---|--------|------|---------|
| 37 | 文献综述 Agent | 侧边栏 → 科研助手 → "科研 Agent" → 选择"文献综述" | 流式生成综述报告 |
| 38 | 论文写作 Agent | 同上 → "论文写作" | 分章节撰写论文 |
| 39 | 实验设计 Agent | 同上 → "实验设计" | 生成假设+方案+代码 |
| 40 | 同行评审 Agent | 同上 → "同行评审" | 5 维评分+详细评审 |
| 41 | 多视角讨论 | 同上 → "多视角讨论" | 4 视角辩论+综合分析 |

### 2.2 FTS5 全文索引

| # | 测试项 | 入口 | 预期结果 |
|---|--------|------|---------|
| 42 | FTS5 搜索 | API: `GET /api/papers/fts-search?q=xxx` | 返回匹配论文，比 LIKE 查询更快 |
| 43 | FTS5 重建 | API: `POST /api/papers/fts-rebuild` | 索引重建成功 |
| 44 | LIKE 降级 | FTS5 不可用时 | 自动降级到 LIKE 查询 |

### 2.3 向量语义搜索

| # | 测试项 | 入口 | 预期结果 |
|---|--------|------|---------|
| 45 | 混合搜索 | API: `GET /api/papers/hybrid-search?q=xxx` | FTS5 + 向量 RRF 融合结果 |
| 46 | 向量构建 | API: `POST /api/papers/build-vectors` | sentence-transformers + FAISS 索引构建 |

### 2.4 引用图谱

| # | 测试项 | 入口 | 预期结果 |
|---|--------|------|---------|
| 47 | 引用关系构建 | API: `POST /api/citations/build` | 从论文 DOI 解析引用关系 |
| 48 | 正向引用 | API: `GET /api/citations/{id}/references` | 返回参考文献列表 |
| 49 | 反向引用 | API: `GET /api/citations/{id}/citing` | 返回被引论文列表 |
| 50 | 引用统计 | API: `GET /api/citations/stats` | 引用关系统计 |

### 2.5 主题聚类

| # | 测试项 | 入口 | 预期结果 |
|---|--------|------|---------|
| 51 | 主题构建 | API: `POST /api/topics/build` | BERTopic 聚类成功 |
| 52 | 主题概览 | API: `GET /api/topics` | 返回主题列表 |
| 53 | 主题论文 | API: `GET /api/topics/{id}/papers` | 返回主题下论文 |

### 2.6 其他 v3.5.0 特性

| # | 测试项 | 入口 | 预期结果 |
|---|--------|------|---------|
| 54 | 分层阅读 | 文献库 → 论文详情 → 元数据/摘要/全文切换 | 三层内容正确显示 |
| 55 | 批量 BibTeX/RIS 导出 | 文献库 → API: `GET /api/papers/export?fmt=bibtex` | 导出内容格式正确 |
| 56 | DOCX 导出 | API: `POST /api/export/docx` | Markdown → DOCX 转换成功 |
| 57 | 工作区 CRUD | API: `GET/POST /api/workspaces` | 创建/查询工作区 |
| 58 | 行为埋点 | API: `POST /api/metrics/event` | 事件记录成功 |
| 59 | 研究洞察 | API: `GET /api/insights` | 返回热词/高频阅读/趋势 |

---

## 三、基础功能回归测试

| # | 测试项 | 入口 | 预期结果 |
|---|--------|------|---------|
| 60 | 应用启动 | 双击 `AI-Nexus-Assistant.exe` | 正常启动，侧边栏+页面加载 |
| 61 | 任务管理 | 侧边栏 → 任务与日程 | 创建/完成/删除任务 |
| 62 | 文献搜索 | 侧边栏 → 文献检索 → 输入关键词 | 7 源搜索返回结果 |
| 63 | AI 对话 | 侧边栏 → AI 对话 → 输入消息 | 流式回复，工具调用正常 |
| 64 | 知识库 | 侧边栏 → IDEA | 卡片 CRUD，标签管理 |
| 65 | 试验管理 | 侧边栏 → 试验管理 | 实验 CRUD，结果记录 |
| 66 | 设置 | 侧边栏 → 设置 | 模型配置、主题切换、备份 |
| 67 | 时钟窗口 | 关闭主窗口 | 辉光管时钟正常显示 |
| 68 | 数据备份 | 设置 → 手动备份 | 备份文件生成 |

---

## 四、v3.6.0 Issue 修复验证

| # | 测试项 | 入口 | 预期结果 | 状态 |
|---|--------|------|---------|------|
| 69 | Word Hopper 中文释义 | 游戏机 → Word Hopper | 所有单词释义为中文，无英文 | ⬜ |
| 70 | 音乐列表排序 | 音乐页 → 排序按钮 | 按文件名/标题/艺术家排序，升序/降序 | ⬜ |
| 71 | Reader 翻页 UI | 书架 → 打开 EPUB/TXT/MD | 左右点击/键盘方向键翻页，翻页动画 | ⬜ |
| 72 | Reader 护眼模式 | Reader → 太阳图标 | 暖色调背景，护眼效果 | ⬜ |
| 73 | 写作工作台持久化 | 写作 → 输入内容 → 切换页面 → 返回 | 内容保留不丢失 | ⬜ |
| 74 | Reader PDF 支持 | 书架 → 打开 PDF 文件 | PDF 文本提取，分页阅读 | ⬜ |
| 75 | open-webSearch 配置指南 | 设置 → 联网搜索服务 → 展开指南 | 显示便携版/安装程序配置说明 | ⬜ |
| 76 | MinerU 配置指南 | 设置 → PDF 转换引擎 → 展开指南 | 显示安装/卸载/常见问题 | ⬜ |
| 77 | 游戏进度保存 | 游戏 → 玩一段时间 → ESC 退出 → 重新进入 | 提示恢复进度或重新开始 | ⬜ |
| 78 | 游戏历史最高分 | 游戏 → 完成一局 → 按 H | 显示前 7 名排行榜 | ⬜ |
| 79 | IDEA 网页抓取分类 | IDEA → 抓取导入 → 切换到"网页抓取" | 显示导入的网页卡片 | ⬜ |
| 80 | CLAUDE.md 环境配置 | 查看 CLAUDE.md | 环境配置说明完整 | ⬜ |

---

## 五、v3.7.0 Reader 修复验证

| # | 测试项 | 入口 | 预期结果 | 状态 |
|---|--------|------|---------|------|
| 81 | EPUB 堆栈溢出修复 | 书架 → 打开大型 EPUB 文件 | 正常解析，无 RangeError | ⬜ |
| 82 | EPUB 空白页修复 | 书架 → 打开各种 EPUB 文件 | 内容正常显示，无空白页 | ⬜ |
| 83 | PDF 原页渲染 | 书架 → 打开 PDF 文件 | 显示原始 PDF 页面（保留格式/图片/公式） | ⬜ |
| 84 | PDF 翻页 | PDF Reader → 点击左右区域 | 正确翻页，页码更新 | ⬜ |
| 85 | PDF 页码选择 | PDF Reader → 底部下拉框 | 可选择任意页码跳转 | ⬜ |

---

## 测试环境要求

- **校园网环境**: 测试出版社 PDF 拉取（#1-6）
- **Node.js**: 测试联网搜索服务
- **GPU（可选）**: 测试向量索引构建（#46）和主题聚类（#51）
- **MinerU（可选）**: 测试 PDF→Markdown 转换（#9-10）

---

## 测试记录

| 日期 | 测试人 | 测试范围 | 通过/失败 | 备注 |
|------|--------|---------|----------|------|
| | | | | |

