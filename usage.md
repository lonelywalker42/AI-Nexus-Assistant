# 使用说明

## 环境要求

- Python 3.10+
- Windows 10/11（推荐，系统托盘功能依赖 Windows）
- 约 250MB 磁盘空间（基础依赖）

## 安装

### 基础安装

```bash
cd D:/ai_coding_research
pip install -e .
```

### 完整安装（含 ChromaDB 向量搜索）

```bash
pip install -e ".[full]"
```

> 完整安装会额外安装 ChromaDB + sentence-transformers（约 400MB），首次加载嵌入模型需要 5-10 秒。

## 启动

```bash
python main.py
```

启动后显示主窗口，左侧侧边栏导航，右侧内容区域。

---

## 功能说明

### 📋 任务与日程

**日历视图**
- 左侧显示月历，有待办的日期会显示圆点标记
- 橙色圆点 = 有待办未完成
- 绿色圆点 = 全部已完成
- 点击日期查看该日待办

**添加待办**
1. 在右侧输入框输入任务内容
2. 选择优先级（普通/低/高/紧急）
3. 点击"添加"或按 Enter

**管理待办**
- 点击 ○ 按钮切换完成状态
- 点击 ✕ 按钮删除任务
- 使用过滤器切换：全部 / 待办 / 已完成

**统计面板**
- 左下角显示当日统计（总待办/已完成）
- 月度完成率自动计算

---

### 📚 文献管理

#### 关键词检索

1. 切换到"文献管理"页面
2. 在关键词组中输入搜索词
   - 组内关键词用 AND 连接
   - 不同组之间用 OR 连接
3. 勾选数据源（默认：OpenAlex + arXiv + Semantic Scholar）
4. 点击"搜索"

搜索在后台线程执行，不会阻塞界面。结果以卡片形式展示，包含：
- 标题、作者、年份、期刊
- 摘要预览
- 来源标签
- 操作按钮（详情/引用/AI总结）

#### 标题检索

1. 切换到"标题检索" Tab
2. 每行输入一个标题（支持不完整/模糊标题）
3. 点击"批量检索"

#### AI 综述

1. 先通过关键词检索获取文献列表
2. 切换到"AI 综述" Tab
3. 点击"生成综述"
4. AI 会基于检索结果生成结构化综述

> 需要先在设置中配置 AI 模型。

#### 选题讨论

1. 切换到"选题讨论" Tab
2. 输入研究方向或兴趣描述
3. 点击"开始讨论"
4. AI 返回选题建议 + 关键词组（可一键发送到关键词检索）

#### 历史记录

所有搜索、综述、选题操作自动保存到历史记录，最多保留 100 条。

---

### ⚙️ 设置

#### AI 模型配置

1. 点击"添加模型"
2. 填写：
   - **名称**：显示名称，如 "DeepSeek-R1"
   - **Base URL**：API 端点，如 `https://api.deepseek.com/v1`
   - **API Key**：你的 API Key
   - **模型名称**：如 `deepseek-reasoner`
   - **协议**：选择 `openai` 或 `anthropic`
   - **用途**：`all`（通用）/ `summary`（总结）/ `review`（综述）/ `chat`（对话）
3. 点击确定

**支持的 AI 服务**：
| 服务 | Base URL | 协议 |
|------|----------|------|
| DeepSeek | `https://api.deepseek.com/v1` | openai |
| 小米 MiMo | `https://api.xiaomimimo.com/v1` | openai |
| OpenAI | `https://api.openai.com/v1` | openai |
| Anthropic | `https://api.anthropic.com` | anthropic |
| Ollama (本地) | `http://localhost:11434/v1` | openai |

> DeepSeek-R1 等思考模型的推理过程会以折叠形式显示。

#### 主题切换

在"主题设置"中选择"暗色"或"亮色"，即时生效。

#### 搜索设置

- **默认数据源**：勾选常用的学术数据库
- **每源最大结果数**：10-200，默认 50

#### 数据管理

- **从 ai-literature 导入**：导入文献库和搜索历史（需要先导出 JSON）

---

## 系统托盘

- 关闭窗口时自动最小化到系统托盘（不退出）
- 双击托盘图标恢复窗口
- 右键菜单：打开主窗口 / 退出
- 托盘 tooltip 显示应用名称

---

## 数据存储

所有数据存储在 `data/` 目录下：

```
data/
├── nexus.db          # SQLite 主数据库
├── nexus.db-wal      # WAL 日志
├── nexus.db-shm      # 共享内存
├── backups/          # 自动备份
├── exports/          # 导出文件
└── uploads/          # 上传的文件
```

---

## 快捷键（计划中）

| 快捷键 | 功能 |
|--------|------|
| Ctrl+K | 命令面板（全局搜索） |
| Ctrl+N | 添加新待办 |
| Ctrl+1~6 | 切换侧边栏页面 |

---

## 常见问题

**Q: 搜索没有结果？**
A: 检查网络连接。部分数据源（如 Google Scholar）可能需要代理。Scopus 需要 API Key。

**Q: AI 功能不工作？**
A: 在设置中添加 AI 模型配置，确保 API Key 正确。DeepSeek 等服务可能需要充值。

**Q: 如何备份数据？**
A: 复制 `data/nexus.db` 文件即可。自动备份功能将在 Phase 3 实现。

**Q: ChromaDB 安装失败？**
A: ChromaDB 是可选依赖。基础安装不需要它。如果需要向量搜索功能，确保 Python 版本 >= 3.10 且有足够的磁盘空间。
