# 使用说明

## 环境要求

- Python 3.10+
- Windows 10/11（推荐，系统托盘功能依赖 Windows）
- 约 250MB 磁盘空间（基础依赖）

## 安装

### 基础安装

```bash
git clone https://github.com/lonelywalker42/AI-Nexus-Assistant.git
cd AI-Nexus-Assistant
pip install -e .
```

### 完整安装（含 ChromaDB 向量搜索）

```bash
pip install -e ".[full]"
```

> 完整安装会额外安装 ChromaDB + sentence-transformers（约 400MB），首次加载嵌入模型需要 5-10 秒。可在设置中按需启用。

## 启动

```bash
python main.py
```

启动后显示主窗口，左侧侧边栏导航，右侧内容区域，底部状态栏显示时钟。

---

## 功能说明

### 📊 仪表盘

应用的首页，展示全局概览：

- **6 个统计卡片**：今日待办、月度完成率、文献总数、进行中试验、规划中试验、知识卡片数
- **近期活动流**：最近完成的任务、搜索历史、试验更新
- 点击右上角"🔄 刷新"更新数据

### 📋 任务与日程

**日历视图**
- 左侧显示月历，有待办的日期会显示圆点标记
- 橙色圆点 = 有待办未完成
- 绿色圆点 = 全部已完成
- 点击日期查看该日待办

**添加待办**
1. 在右侧输入框输入任务内容
2. 选择优先级（普通/低/高/紧急）
3. 点击"添加"或按 Enter

**管理待办**
- 点击 ○ 按钮切换完成状态
- 点击 ✕ 按钮删除任务
- 使用过滤器切换：全部 / 待办 / 已完成

**统计面板**
- 左下角显示当日统计（总待办/已完成）
- 月度完成率自动计算

---

### 📚 文献管理

#### 关键词检索

1. 切换到"文献管理"页面
2. 在关键词组中输入搜索词
   - 组内关键词用 AND 连接
   - 不同组之间用 OR 连接
3. 勾选数据源（默认：OpenAlex + arXiv + Semantic Scholar）
4. 点击"搜索"

搜索在后台线程执行，不会阻塞界面。结果以卡片形式展示，包含：
- 标题、作者、年份、期刊
- 摘要预览
- 来源标签
- 操作按钮（详情/引用/AI总结）

#### 标题检索

1. 切换到"标题检索" Tab
2. 每行输入一个标题（支持不完整/模糊标题）
3. 点击"批量检索"

#### AI 综述

1. 先通过关键词检索获取文献列表
2. 切换到"AI 综述" Tab
3. 点击"生成综述"
4. AI 会基于检索结果生成结构化综述（引言→研究现状→方法分类→趋势→展望）

> 需要先在设置中配置 AI 模型。

#### 选题讨论

1. 切换到"选题讨论" Tab
2. 输入研究方向或兴趣描述
3. 点击"开始讨论"
4. AI 返回选题建议 + 关键词组（可一键发送到关键词检索）

#### 历史记录

所有搜索、综述、选题操作自动保存到历史记录，最多保留 100 条。

---

### 🧪 试验管理

**布局**：左侧试验列表 + 右侧详情（3 个 Tab）

**新建试验**
1. 点击左下角"➕ 新建试验"
2. 输入试验名称

**基本信息 Tab**
- 状态选择：planning / running / completed / suspended
- 背景、目标、实验设置（自动保存）

**试验结果 Tab**
- 点击"➕ 添加结果"添加新版本
- 每个版本记录：描述、参数(JSON)、代码片段、结果数据、结论
- 版本号自动递增
- 双击行可编辑
- 点击"📄 导出 Markdown"生成完整试验报告

**参数对比**
- 结果表格中"参数"列显示关键参数变化
- 不同版本的参数差异一目了然

---

### 🧠 知识库

**布局**：左侧筛选面板 + 右侧卡片网格

**新建知识卡片**
1. 点击左下角"➕ 新建卡片"
2. 填写标题、摘要、分类路径、标签

**浏览与筛选**
- 搜索框：按标题/摘要/笔记模糊搜索
- 来源过滤：全部 / 手动创建 / 文献导入 / AI对话
- 标签过滤：点击左侧标签列表

**卡片详情**
- 点击卡片打开详情面板
- 编辑摘要和用户笔记
- 点击星星设置 1-5 星评分
- 点击"💾 保存"保存修改

**从文献生成**
- 在文献管理页面，可将文献一键保存为知识卡片

**从对话生成**
- 在 AI 对话页面，点击"💾 存为卡片"保存 AI 回复

---

### 💬 AI 对话

**布局**：左侧会话列表 + 右侧对话区域

**新建对话**
1. 选择 AI 模型（左上角下拉框）
2. 点击"➕ 新对话"

**发送消息**
- 在底部输入框输入消息
- 按 Enter 发送，Shift+Enter 换行
- AI 回复实时流式输出

**Thinking 折叠**
- DeepSeek-R1 等思考模型的推理过程默认折叠显示
- 点击 thinking 区域可展开/折叠

**写作辅助**
- 选中文本（或直接在输入框输入），点击快捷按钮：
  - 📝 润色：改进学术表达
  - 🌐 翻译：中英互译
  - 📐 LaTeX：转为 LaTeX 代码
  - 📋 摘要：生成中英文摘要

**跨模块联动**
- 📚 引用文献：将文献库中的论文作为上下文
- 🧪 引用试验：将试验记录作为上下文
- 💾 存为卡片：将 AI 回复保存为知识卡片

**参考文献格式化**
- 在文献管理页面选中文献，点击"📋 引用"获取 GB/T 7714 格式引用

---

### 🔍 命令面板

按 **Ctrl+K** 唤出命令面板：

- 输入关键词搜索所有模块（任务/文献/试验/知识卡片）
- 输入页面名称快速跳转（如"设置"、"对话"）
- ↑↓ 键导航，Enter 选择，Esc 关闭

---

### ⏰ 桌面时钟

**状态栏时钟**
- 主窗口底部状态栏右侧显示 HH:MM:SS 时钟

**浮动时钟**（计划中）
- 可从状态栏弹出为独立浮动窗口
- 辉光管/机械表双模式
- 双击切换模式，拖拽移动，滚轮缩放

---

### ⚙️ 设置

#### AI 模型配置

1. 点击"添加模型"
2. 填写：
   - **名称**：显示名称，如 "DeepSeek-R1"
   - **Base URL**：API 端点，如 `https://api.deepseek.com/v1`
   - **API Key**：你的 API Key
   - **模型名称**：如 `deepseek-reasoner`
   - **协议**：选择 `openai` 或 `anthropic`
   - **用途**：`all`（通用）/ `summary`（总结）/ `review`（综述）/ `chat`（对话）
3. 点击确定

**支持的 AI 服务**：

| 服务 | Base URL | 协议 | 推荐用途 |
|------|----------|------|----------|
| DeepSeek-V3 | `https://api.deepseek.com/v1` | openai | 对话/综述 |
| DeepSeek-R1 | `https://api.deepseek.com/v1` | openai | 推理/综述 |
| 小米 MiMo | `https://api.xiaomimimo.com/v1` | openai | 快速总结 |
| OpenAI | `https://api.openai.com/v1` | openai | 通用 |
| Anthropic | `https://api.anthropic.com` | anthropic | 通用 |
| Ollama (本地) | `http://localhost:11434/v1` | openai | 离线使用 |

> DeepSeek-R1 等思考模型的推理过程会以折叠形式显示。

#### 主题切换

在"主题设置"中选择"暗色"或"亮色"，即时生效。

#### 搜索设置

- **默认数据源**：勾选常用的学术数据库
- **每源最大结果数**：10-200，默认 50

#### 数据管理

- **从 ai-literature 导入**：导入文献库和搜索历史（需要先导出 JSON）
- **手动备份**：立即创建数据库备份

---

## 系统行为

### 系统托盘

- 关闭窗口时自动最小化到系统托盘（不退出）
- 双击托盘图标恢复窗口
- 右键菜单：打开主窗口 / 退出

### 自动备份

- 每次启动应用时自动执行备份
- 保留策略：1 个月备份 + 1 周备份 + 6 日备份
- 过期备份自动清理
- 备份文件位于 `data/backups/`

---

## 数据存储

所有数据存储在 `data/` 目录下：

```
data/
├── nexus.db          # SQLite 主数据库
├── nexus.db-wal      # WAL 日志
├── nexus.db-shm      # 共享内存
├── backups/          # 自动备份
├── exports/          # 导出文件
└── uploads/          # 上传的文件
```

---

## 快捷键

| 快捷键 | 功能 |
|--------|------|
| Ctrl+K | 命令面板（全局搜索） |
| Enter | 发送消息（对话页面） |
| Shift+Enter | 换行（对话页面） |
| Esc | 关闭对话框 |

---

## 构建 EXE

```bash
python build.py
```

输出位于 `dist/AI-Nexus-Assistant/` 目录。

---

## 常见问题

**Q: 搜索没有结果？**
A: 检查网络连接。部分数据源（如 Google Scholar）可能需要代理。Scopus 需要 API Key。

**Q: AI 功能不工作？**
A: 在设置中添加 AI 模型配置，确保 API Key 正确。DeepSeek 等服务可能需要充值。

**Q: 如何备份数据？**
A: 应用启动时会自动备份。也可以在设置中点击"手动备份"。备份文件位于 `data/backups/`。

**Q: ChromaDB 安装失败？**
A: ChromaDB 是可选依赖。基础安装不需要它。如果需要向量搜索功能，确保 Python 版本 >= 3.10。

**Q: 如何从旧版本迁移数据？**
A: 在设置页面点击"从 ai-literature 导入"，选择导出的 JSON 文件。

**Q: 时钟组件不显示？**
A: 时钟显示在主窗口底部状态栏右侧。如果看不到，请检查窗口是否足够大。
